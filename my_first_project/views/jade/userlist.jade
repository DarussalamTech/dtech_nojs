extends layout

block content
    h1.
        User List
    ul
        each user, i in userlist
            li
                a(href='viewuser/'+user._id)= user.username +'--->'+ user.email
                        